DATABASE_URL="postgresql://postgres:postgres@localhost:5432/aikol"

# 32-byte key; support base64 or hex. Keep this constant after go-live.
TOKEN_ENCRYPTION_KEY="replace-with-32-byte-secret"

# Protect /api/cron/publish in production
CRON_SECRET="replace-with-cron-secret"

# Set 1 in local development when you do not want to hit real X API
MOCK_X_API=1

# Optional credentials for future OAuth onboarding flow
AUTH_TWITTER_ID=""
AUTH_TWITTER_SECRET=""
